# 硬话软说 · 部署配置

部署到 Vercel 前需要的环境变量:

```
DOUBAO_API_KEY=your_volcengine_ark_api_key
DOUBAO_ENDPOINT=https://ark.cn-beijing.volces.com/api/v3/chat/completions
DOUBAO_MODEL=doubao-lite-32k
```

没配这三个变量也能跑,会自动降级到本地 Mock 回复。

## 火山引擎 Ark 接入步骤

1. https://www.volcengine.com 注册/登录
2. 开通"火山方舟"(Ark),实名认证
3. 控制台 → 在线推理 → 创建端点,选 `doubao-lite-32k`(性价比高、中文强)
4. 创建 API Key
5. 把上面的三个变量填到 Vercel 项目设置 → Environment Variables